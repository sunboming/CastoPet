# 项目中仍旧存在的一些待修改的问题

## 1. 数据结构问题

- `PetActionDefinition` 数据类属性存在冗余，逻辑混乱
- `PetActionKind` 枚举数据类逻辑不合适，不利于后续拓展

## 2. 逻辑后置问题（某些判断逻辑放入了执行器中）

- `PetAnimationController` 动画控制器类中的某些成员函数，将判断是否存在动画图片资源这一行为，放入了执行逻辑中

## 3. 类设计不合理，不利于拓展

- `PetAnimationController` 动画控制器类中将每个动画的启动、推进、停止都单独作为成员函数
